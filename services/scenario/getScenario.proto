syntax = "proto3";

import "greet.proto";


service GetScenario {
    // Sends a greeting
    rpc Hello (HelloRequest) returns (Scenario);
  }

  service GetScenario2 {
    // Sends a greeting
    rpc Hello (HelloRequest) returns (Scenario);
  }